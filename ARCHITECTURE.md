# 🏛️ АРХИТЕКТУРА И ЛОГИКА ИГРОВОГО ДВИЖКА (CORE GAME LOOP)
## Проект: «Приключения Котёнка» (Интерактивные сказки-тренажёры)

---

## 1. ОБЩАЯ КАРТА ЭКРАНОВ (FLOW СОСТОЯНИЙ)

```text
[ ЭКРАН 1: ГЛАВНЫЙ КАТАЛОГ СЕТОВ ]
  │  (Здесь есть переключатель темы ☀️/🌿 и звук 🔊)
  ▼ выбор сета (4 цветастых блока)
[ ЭКРАН 2: ВЫБОР СКАЗКИ В СЕТЕ ]
  │  (4 цветастые карточки сказок, кнопка «Назад»)
  ▼ выбор сказки
[ ЭКРАН 3: ОБЛОЖКА И КНОПКА СТАРТА ]
  │  (Аватар героя, описание для родителей, кнопка «▶️ Начать сказку»)
  │  *Решает проблему автоблокировки звука браузерами*
  ▼ клик по кнопке старта
[ ЭКРАН 4: ИГРОВАЯ СЦЕНА КВЕСТА (ЦЕЛЕВОЙ ЦИКЛ ИЗ 7 ШАГОВ) ]
  ▼ завершение 7 шага
[ ЭКРАН 5: ФИНАЛЬНАЯ ПОХВАЛА И ВОЗВРАТ ]
```

---

## 2. ПОШАГОВЫЙ ЦИКЛ СЦЕНЫ (СЦЕНАРИЙ «ПРИЧИНА И СЛЕДСТВИЕ»)

Каждый шаг квеста строго разделен на **3 последовательные фазы**:

### 🔹 Фаза 1: «ДО» (Постановка задачи и вопрос)
1. **Иллюстрация:** Показывается картинка исходного состояния (`beforeSvg`) — *например: шторки закрыты, тарелочка пустая, лапки босые, капает дождик*.
2. **Кнопки выбора:** Полностью скрыты (`opacity: 0; pointer-events: none`).
3. **Индикатор:** Внизу горит подсказка: *«🎧 Слушаем рассказчика...»*.
4. **Речь диктора:** Озвучивается текст и логопедический вопрос (`speechText`).
5. **Событие окончания речи (`utterance.onend`):**
   * Индикатор скрывается.
   * Контейнер кнопок плавно всплывает (`opacity: 1; transform: translateY(0)`).
   * Рамка кнопок начинает деликатно переливаться спектром раз в 10 секунд (`@keyframes shimmerEvery10Sec`).

### 🔹 Фаза 2: Действие ребёнка (Выбор)
1. **Тап по кнопке выбора:**
   * Срабатывает мягкий звук (колокольчик `playChime()` или стук `playKnock()`).
   * Кнопки выбора мгновенно скрываются (блокируется повторный случайный клик).
   * **Значок сделанного выбора (`badgeIcon`)** мгновенно улетает в верхнюю плашку в соответствующий слот и сохраняется до конца сказки.
   * Если это шаг с рюкзачком (Шаг 4) — сохраняется состояние `backpackItem: 'umbrella' | 'fish'`.

### 🔹 Фаза 3: «ПОСЛЕ» (Награда, следствие и закрепление)
1. **Смена иллюстрации:** Картинка сцены плавно переключается на состояние «ПОСЛЕ» (`afterSvg`) — *котик уже под раскрытым зонтом / с белыми молочными усиками / в сапожках в лужице*.
2. **Текст облачка речи:** Заменяется на эмоциональную похвалу (`reactionSpeech`) и звукоподражание (`reactionSoundText`).
3. **Озвучка реакции:** Диктор воодушевленно комментирует результат (*«ХЛОП! Раскрыли зонтик! Тепло и сухо!»*).
4. **Пауза для восприятия:** После окончания фразы даётся пауза ~1.5 секунды, чтобы ребёнок разглядел результат своего решения.
5. **Переход:** Движок переключает `currentStep += 1` и запускает следующий шаг (или открывает экран поздравления).

---

## 3. СТРУКТУРА ОБЪЕКТА СЦЕНЫ В ДАННЫХ (DATA CONTRACT)

```javascript
{
  step: 2,
  headerText: "Котёнок: Завтрак",
  beforeSvg: "<svg>...</svg>",        // Картинка ДО
  speechText: "Чем мы сегодня угостим Котёнка?",
  soundEffectText: "«Нюх-нюх! Мур-р-р!»",
  choices: [
    {
      id: "milk",
      icon: "🥛",
      title: "Тёплое молочко",
      soundType: "chime",
      badgeIcon: "🥛",
      afterSvg: "<svg>...</svg>",     // Картинка ПОСЛЕ
      reactionSpeech: "Тёплое молочко! Лакаем язычком: Хлюп-хлюп-хлюп!...",
      reactionSoundText: "«Хлюп-хлюп! М-м-м!»"
    }
  ]
}
```

---

## 4. UI И ТЕМАТИЧЕСКИЕ ПРАВИЛА
1. **Кнопка смены темы (☀️/🌿):** Отображается **только** на Главном экране каталога сетов.
2. **Окно игры:** Всегда держит скруглённые рамки даже на мобильных устройствах (min-height, border-radius: 24–32px).
3. **Цветастость карточек:** Каждый сет и каждая сказка окрашены в свой индивидуальный нежный пастельный градиент.
